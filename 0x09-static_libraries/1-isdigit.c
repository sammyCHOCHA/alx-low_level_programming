#include "main.h"

/**
*_isdigit - This  checks for a digit (0 through 9)
*@c:This is the  int to be checked
*Return: This retun  1 if c is a digit, 0 otherwise
*/

int _isdigit(int c)

{
	return	(c >= '0' && c <= '9');
}
