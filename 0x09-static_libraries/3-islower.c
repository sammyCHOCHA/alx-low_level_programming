#include "main.h"

/**
 * _islower - This checks for lowercase character
 * @c: This is the character to check
 * Return: To return 1 if c is lowercase, 0 otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
